# Architecture

> This document will be filled in as the system is built, milestone by milestone.
> Placeholder created in Milestone 1 to establish the documentation structure early.

## Planned Sections
- System overview diagram
- Request lifecycle (upload → preprocessing → inference → explainability → response)
- `MediaAnalyzer` abstraction and how video support will plug in later
- Data flow for prediction history persistence
- Key design decisions and trade-offs
