# API Documentation

> Populated once the `/predict`, `/history`, and `/health` endpoints exist (Milestones 2, 5, 6).
> FastAPI also auto-generates interactive docs at `/docs` (Swagger) and `/redoc` once the app is running.
