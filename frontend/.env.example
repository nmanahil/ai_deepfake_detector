# Copy to .env.local for local overrides (gitignored).
#
# Which detection engine the UI talks to:
#   mock  – in-browser simulated engine (default, no backend required)
#   http  – a FastAPI ML backend implementing docs/detection-api.md
NEXT_PUBLIC_DETECTION_PROVIDER=mock
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
